1. 정규화 과정 
- 반복 집합이 있는 비정규 릴레이션
- 제1정규형 릴레이션(모든 속성값이 원잣값으로 구성됨) -> 반복 집합을 제거함
- 제2정규형 릴레이션(제1정규형이면서 키가 아닌 모든 속성이키에 완전 함수적으로 종속) -> 부분 - 함수의 종속성을 제거함
- 제3정규형 릴레이션(키에 대해서 직접적으로 함수 종속) -> 이행적 함수 종속을 제거함
- BCNF 릴레이션(모든 결정자가 후보키로 구성됨) -> 후보키가 아닌 결정자를 제거함
